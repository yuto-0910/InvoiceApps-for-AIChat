{
  "name": "invoice-mcp",
  "version": "1.0.0",
  "description": "請求書生成ローカルMCPサーバー",
  "type": "module",
  "scripts": {
    "build": "tsup src/server.ts --format esm --outDir dist --no-splitting",
    "dev": "tsx src/server.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "gray-matter": "^4.0.3"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsup": "^8.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
